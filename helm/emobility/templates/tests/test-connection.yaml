apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "emobility.fullname" . }}-test-connection"
  labels:
    {{- include "emobility.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "emobility.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
